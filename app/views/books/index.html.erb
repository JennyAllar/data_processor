
<h1>Welcome to the Data Processor!</h1>

<%= button_to "Click Here", book_fetch_xlsx_data_path %>
<br><br><br>
<%= form_tag import_books_path, multipart: true do %>
  <%= file_field_tag :file %>
  <%= submit_tag "Import" %>
<% end %>

<br><br><br>
<div class="caption">Inventory</div>
<div id="table">
  <div class="header-row row">
    <span class="cell primary"><%= sortable "title", "Title" %></span>
    <span class="cell"><%= sortable "author", "Author" %></span>
    <span class="cell"><%= sortable "description", "Description" %></span>
    <span class="cell"><%= sortable "edition", "Edition" %></span>
    <span class="cell"><%= sortable "year", "Copyright Year" %></span>
    <span class="cell"><%= sortable "price", "List Price" %></span>
  </div>
  <% @books.each do |book| %>

    <div class="row">
      <input type="radio" name="expand">
    <span class="cell primary" data-label="Title"><%= book.title %>
</span>
      <span class="cell" data-label="Author"><%= book.author %></span>
      <span class="cell" data-label="Description"><%= book.description %></span>
      <span class="cell" data-label="Edition"><%= book.edition %></span>
      <span class="cell" data-label="Year"><%= book.year %></span>
      <span class="cell" data-label="Price"><%= book.price %></span>

    </div>
  <% end %>







</div>